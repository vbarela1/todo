<h1>
  Editing: <%= @todo.title %>
</h1>

<%= form_for @todo, url: todolists_path do |f| %>
 <%= f.label :title %>
 <%= f.text_field :title %>

<br />

 <%= f.label :description %>
 <%= f.text_field :description %> 

<br />

 <%= f.submit %>
<% end %> 

<%= link_to 'Back', todo_show_path(@todo) %>